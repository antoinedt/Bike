# Add project specific ProGuard rules here.
# minifyEnabled is off for the debug/first-version release build, so this is
# currently unused, but kept so enabling minification later doesn't start blind.
